@using ReHouse.Utils.DataBase.AdvertParams
@model List<NewBuilding>

@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <div class="data-item">
            <li>
                <div class="redimg"><img src="~/Content/images/newred.png"></div>
                <a href="#" class="fix"><img src="~/Content/images/prod1.jpg" width="250" height="212" alt="" /></a>
                <div class="min-content">
                    <h3>@item.Name</h3>
                    <h4>@item.Adress <br />Сдача: @item.ExpluatationDate.Name</h4>
                    <p class="cost">Цена: от @item.Price грн/м<sup>2</sup></p>
                    <a href="~/NewBuilding/Detail/@item.Id" id="follow">❯</a>
                </div>
            </li>
        </div>
    }
}