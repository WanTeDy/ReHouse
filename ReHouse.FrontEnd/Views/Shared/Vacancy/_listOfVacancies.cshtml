@using ReHouse.Utils.DataBase.Vacancies
@model List<Vacancy>

@if (Model.Count > 0)
{
    foreach (var vacancy in Model)
    {
        <div class="call_us">
            <p>@vacancy.Title</p>
        </div>
        <p>@Html.Raw(vacancy.Description)</p>        
    }
}