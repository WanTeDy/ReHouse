@using ITFamily.FrontEnd.Models
@{
    var token = ViewBag.Token as string;
    var recov = new RecoveryPasswordModel
    {
        TokenHash = token
    };
}
@Html.Partial("RecoveryPasswordPartial", recov)