@using ReHouse.Utils.DataBase.Common
@model List<SeoParam>

@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>
                <div class="prod_info">
                    <h3>@item.FullUrl</h3>
                </div>
            </td>
            <td>@item.Title</td>
            <td>@item.Description</td>
            <td>@item.Keywords</td>
            <td>
                <div class="edit_del">
                    <form method="LINK" action="/cabinet/seoparametrs/edit/@item.Id">
                        <button>Редактировать</button>
                    </form>                    
                </div>
            </td>
        </tr>
    }
}