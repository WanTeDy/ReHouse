@using ReHouse.Utils.Helpers
@using ReHouse.Utils.DataBase.News;
@model List<Article>
@{
    Layout = "~/Areas/Cabinet/Views/Shared/_Layout.cshtml";
}
@Html.Partial("_navigation", "news")

<div id="adminka2" class="tabset_content">
    <div class="headline">
        <h2>Добавленные новости</h2>
        <form method="LINK" action="/cabinet/news/add">
            <button>Добавить новость ►</button>
        </form>
    </div>

    @if (!ViewBag.NoElements)
    {

        <div class="added">
            <button onclick="deleteChekedProducts();" class="delete_all">Удалить выбранные</button>
            <table class="added" id="data-container">
                @Html.Partial("Article/_listOfArticles", Model)
            </table>
        </div>
        <div>
            <div class="products1">
                <div id="button-more">
                    <a id="more">Больше новостей</a>
                </div>
            </div>
        </div>
        <div id="loading-div">

        </div>
    }

</div>
@section scripts {
    @Scripts.Render("~/bundles/deleteArticles")

    <script>
        var url = "/cabinet/news/load";
        $('#button-more').click(function () {

            var obj = {
        }

            lazyload(obj, url, false);
        });       
    </script>

}